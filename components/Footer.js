import Heading from "./Heading";


const Footer = () => {
    return (
        <footer>
            <Heading tag={'h3'} text={'This is a Footer'}/>
        </footer>

    );
};

export default Footer;